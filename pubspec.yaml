name: serviceflow
description: A new Flutter project.
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Localizations (requiere intl 0.20.2)
  flutter_localizations:
    sdk: flutter

  # Dependencias de tu proyecto
  intl: ^0.20.2
  cupertino_icons: ^1.0.2
  flutter_svg: ^2.0.7  # ✅ Paquete para trabajar con SVG

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true

  assets:
    - assets/images/service_flow_logo.svg  # ✅ Ruta al archivo SVG
